# macOS
.DS_Store
.AppleDouble
.LSOverride
*.swp

# Local settings & config (but track the org's shared skills/commands/agents)
.claude/*
!.claude/skills/
!.claude/commands/
!.claude/agents/
.vscode/mcp.json
node_modules/
.env

# Self-hosted SDK copy (generated by `npm run vendor-sdk`)
vendor/

# Unused / archived material — kept locally, out of the repo
misc/

# Plan-mode dumps (superseded by docs/ts-watch-playbook.md)
Plan/

# Personal editor workspaces
*.code-workspace
